{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome",
        "url": "http://localhost:8080", // Change the port if your server is running on a different port
        "webRoot": "${Emojication}"
      }
    ]
  }
  